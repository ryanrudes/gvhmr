"""Vendored third-party code, frozen and kept byte-for-byte close to upstream.

Each subpackage carries the upstream LICENSE and a README documenting its exact
provenance (repo, tag, files, and the minimal import-path patches applied). See
``docs/PROVENANCE.md`` for the full manifest and the upstream-sync playbook.
"""
