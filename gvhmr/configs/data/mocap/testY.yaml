# definition of lightning datamodule (dataset + dataloader)
_target_: gvhmr.datamodule.mocap_trainX_testY.DataModule

dataset_opts:
  test: ${test_datasets}

loader_opts:
  test:
    batch_size: 1
    num_workers: 0
