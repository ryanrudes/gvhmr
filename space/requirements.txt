# Gradio SDK Space (ZeroGPU). `chumpy` + `gvhmr` are bootstrapped in app.py — chumpy's legacy
# setup.py imports `pip` at build time, which PEP 517 isolation hides on the Gradio builder.
numpy>=1.26
setuptools
wheel
gradio>=4,<6
spaces
